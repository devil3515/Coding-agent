# Core LLM clients
openai>=1.12.0
anthropic>=0.18.0

# CLI & terminal UI
rich>=14.0.0
typer>=0.21.0

# Configuration
PyYAML>=6.0.1

# Database (short-term & long-term memory)
pymongo>=4.7.0

# Token counting
tiktoken>=0.9.0

# AST-based codebase graph (Tree-sitter + language grammars)
tree-sitter>=0.25.2
tree-sitter-python>=0.25.0
tree-sitter-javascript>=0.25.0
tree-sitter-typescript>=0.23.2

# MCP (Model Context Protocol) server bridge
mcp>=1.26.0

# HTTP / async (transitive — pinned for stability)
httpx>=0.28.0
httpx-sse>=0.4.0
anyio>=4.0.0
sniffio>=1.3.0
websockets>=15.0.0
prompt_toolkit
